export const defineObject = <Obj>(definition: Obj) => definition

interface CreateHubspotClientArgs {
    objects: object[]
    authToken: string
}

const createHubspotClient = (args: CreateHubspotClientArgs) => {
}